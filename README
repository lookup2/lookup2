Lookup は Emacs で使える辞書検索ソフトです。CD-ROM 辞書や辞書サーバなど、
様々な辞書から統一された操作で検索が行なえます。

インストール
------------

  % ./configure --with-emacs=emacs \
                --with-lispdir=/your/home/emacs/lisp \
                --infodir=/your/home/emacs/info
  % make install

単に configure && make install とすると、Emacs の site-lisp ディレクトリ
(i.e., /usr/local/share/emacs/site-lisp) の下に lookup というディレクト
リが作られ、そこにファイルがコピーされます。

使い方
------

~/.emacs.el に、例えば次のように書いて下さい。

  (define-key ctl-x-map "l" 'lookup)
  (define-key ctl-x-map "y" 'lookup-region)
  (define-key ctl-x-map "\C-y" 'lookup-pattern)

  (load "lookup-autoloads")
  (setq lookup-search-agents
        '((ndtp "dserver")
          (ndeb "/usr/local/dict/epwing")
          (ndspell)))

M-x lookup で Lookup を起動します。M-x lookup-region で、選択範囲の文字
列を検索します。M-x lookup-pattern で、検索語を入力して検索します。詳し
くは、附属のマニュアルを参照して下さい。

カスタマイズ
------------

~/.lookup/init.el で Lookup の設定をまとめて行なうことが出来ます。

最新情報
--------

  http://openlab.ring.gr.jp/lookup/

Lookup に関する情報交換は、専用のメーリングリストにて行われています。
参加を希望する方は、`lookup-ja-request@ring.gr.jp' 宛てに、本文に
subscribe と書いたメールを送って下さい。
