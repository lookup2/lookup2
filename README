Lookup は Emacs で使える辞書検索ソフトです。CD-ROM 辞書や辞書サーバなど、
様々な辞書から統一された操作で検索が行なえます。

インストール
------------



How to Install
--------------

  % ./configure --with-emacs=emacs \
                --with-lispdir=/your/home/emacs/lisp/lookup \
                --infodir=/your/home/emacs/info
  % make install

How To Use
----------

  (load "lookup-autoloads")
  (setq lookup-search-agents '((ndtp "dserver") (ndspell)))

M-x lookup で Lookup を起動します。始めに利用できる辞書が一覧されます。
M-x lookup-region で、リージョンの文字列を検索します。M-x lookup-pattern 
により、ミニバッファから検索語を入力できます。より詳しくは、附属のマニュ
アルを参照して下さい。

How To Get
----------

  http://openlab.ring.gr.jp/lookup/

Lookup に関する情報交換は、専用のメーリングリストにて行われています。
参加を希望する方は、`lookup-ja-request@ring.gr.jp' 宛てに、本文に
subscribe と書いたメールを送って下さい。

* インストール

lisp/*.el を適当なディレクトリにコピー。必要ならバイトコンパイル。

* 設定方法

** ~/.emacs

(define-key ctl-x-map "l" 'lookup)
(define-key ctl-x-map "y" 'lookup-region)
(define-key ctl-x-map "\C-y" 'lookup-pattern)

(add-to-list 'load-path "/home/kei/emacs/lookup/lisp")
(load "lookup-autoloads")

** ~/.lookup

(setq lookup-search-agents
      '((ndeb "/usr/local/dict/epwing")
	(ndsrd "/usr/local/dict/srd")
	(ndspell)))

(setq lookup-complement-directory "/home/kei/emacs/lookup/comp")
;; 補助ファイルの読み込みは自動で行なわれる。(comp/loaddef.el を参照)

(lookup-set-dictionary-options "ndeb:/usr/local/dict/epwing/CHUJITEN"
  :transformer 'lookup-stemming-search)

* モジュール生成

1. キャッシュファイルを有効にする。

  (setq lookup-cache-file "~/.lookup-cache")

2. Select mode で `C'.

3. モジュールをリネーム/削除するには、キャッシュファイルを直接編集。

* 特定モジュールを利用するコマンド生成

** ~/.emacs

(define-key mode-specific-map "\C-y" 'lookup-japanese)
(autoload 'lookup-japanese "lookup" nil t)

** ~/.lookup

(defun lookup-japanese (pattern)
  (interactive (list (lookup-input-pattern)))
  (lookup-pattern pattern (lookup-get-module "japanese")))
